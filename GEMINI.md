# Gemini — Read AGENTS.md first

The operating contract for this repo is `AGENTS.md`. Read it fully before doing anything, every session.
Do not drift from the mission (one clean end-to-end run) or the method (evidence-first, one bite per
session, branch+PR, pytest stays green, log to PROGRESS.md). Provider policy and honesty boundary are in
`AGENTS.md`; session-01 setup and run steps are in `SESSION-01-KICKOFF.md`.
