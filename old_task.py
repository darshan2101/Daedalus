TASK = """
Build a Python MCP server module that exposes three tools:
1. search_web(query: str) -> returns top 5 results as a list of dicts
2. summarize_url(url: str) -> fetches the URL and returns a 3-sentence summary
3. save_note(title: str, content: str) -> saves content to a local .txt file

Include full working code with proper error handling and type hints.
"""